<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ========================= -->
    <!-- ===== PERMISOS ========= -->
    <!-- ========================= -->

    <!-- Boot / wake -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Usage stats (requiere activación manual en ajustes) -->
    <uses-permission
        android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <!-- Foreground services -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!-- Comms permissions (runtime) -->
    <uses-permission android:name="android.permission.READ_CALL_LOG" />
    <uses-permission android:name="android.permission.READ_SMS" />

    <uses-feature
        android:name="android.hardware.telephony"
        android:required="false" />

    <!-- ========================= -->
    <!-- ===== PACKAGE VISIBILITY -->
    <!-- ========================= -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <!-- ========================= -->
    <!-- ===== APPLICATION ======= -->
    <!-- ========================= -->

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_burnout_app"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_burnout_app_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Burnout_app"
        tools:targetApi="31">

        <!-- ===== ACTIVITIES ===== -->

        <activity
            android:name="gal.uvigo.burnout_app.WelcomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="gal.uvigo.burnout_app.LoginActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name="gal.uvigo.burnout_app.RegisterActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name="gal.uvigo.burnout_app.MainActivity" />
        <activity android:name="gal.uvigo.burnout_app.ProfileActivity" />
        <activity android:name="gal.uvigo.burnout_app.ScreenTimeActivity" />
        <activity android:name="gal.uvigo.burnout_app.MultitaskActivity" />
        <activity android:name="gal.uvigo.burnout_app.NotificationsActivity" />
        <activity android:name="gal.uvigo.burnout_app.CommunicationsActivity" />
        <activity android:name="gal.uvigo.burnout_app.SettingsActivity" />

        <!-- ========================= -->
        <!-- ===== RECEIVERS ========= -->
        <!-- ========================= -->

        <receiver
            android:name=".collectors.ScreenStateReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.SCREEN_ON" />
                <action android:name="android.intent.action.SCREEN_OFF" />
                <action android:name="android.intent.action.USER_PRESENT" />
            </intent-filter>
        </receiver>

        <!-- ========================= -->
        <!-- ===== SERVICES ========= -->
        <!-- ========================= -->

        <service
            android:name="gal.uvigo.burnout_app.collectors.BurnoutNotificationListenerService"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="false">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

    </application>

</manifest>