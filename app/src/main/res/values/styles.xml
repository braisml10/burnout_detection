<!-- styles.xml -->
<resources>

    <!-- Card reutilizable para KPIs -->
    <style name="BurnoutCard" parent="Widget.MaterialComponents.CardView">
        <item name="cardUseCompatPadding">true</item>
        <item name="cardPreventCornerOverlap">true</item>
        <item name="cardCornerRadius">16dp</item>
        <item name="cardElevation">2dp</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
        <!-- Ripple nativo al pulsar -->
        <item name="android:foreground">?attr/selectableItemBackground</item>
    </style>

    <!-- Fondo circular/rounded para iconos dentro de cards -->
    <style name="BurnoutIconBg">
        <item name="android:shape">rectangle</item>
        <item name="android:padding">10dp</item>
        <item name="android:background">@drawable/bg_icon_round</item>
    </style>

    <style name="NightTick">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_weight">1</item>
        <item name="android:gravity">center</item>
        <item name="android:textColor">#94A3B8</item>
        <item name="android:textSize">12sp</item>
    </style>

</resources>
