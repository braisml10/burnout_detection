<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ========================= -->
    <!-- ===== PERMISOS ========= -->
    <!-- ========================= -->

    <!-- Boot / wake -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Usage stats (requiere activación manual en ajustes) -->
    <uses-permission
        android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <!-- Foreground services -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!-- (Opcional) -->
    <!-- <uses-permission android:name="android.permission.READ_PHONE_STATE" /> -->


    <!-- ========================= -->
    <!-- ===== PACKAGE VISIBILITY -->
    <!-- ========================= -->

    <!-- Necesario para que PackageManager vea otras apps (Android 11+) -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>


    <!-- ========================= -->
    <!-- ===== APPLICATION ======= -->
    <!-- ========================= -->

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Burnout_app"
        tools:targetApi="31">

        <!-- ===== ACTIVITIES ===== -->

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ActivityScreenTime" />
        <activity android:name=".ActivityMultitask" />
        <activity android:name=".ActivityNotifications" />


        <!-- ========================= -->
        <!-- ===== RECEIVERS ========= -->
        <!-- ========================= -->

        <receiver
            android:name=".collectors.ScreenStateReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.SCREEN_ON" />
                <action android:name="android.intent.action.SCREEN_OFF" />
                <action android:name="android.intent.action.USER_PRESENT" />
            </intent-filter>
        </receiver>


        <!-- ========================= -->
        <!-- ===== SERVICES ========= -->
        <!-- ========================= -->

        <!-- Notification Listener -->
        <service
            android:name=".collectors.BurnoutNotificationListenerService"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="false">

            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>

        </service>

    </application>

</manifest>